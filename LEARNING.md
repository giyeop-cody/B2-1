# B2-1 학습 노트: 나만의 용돈 기입장 프로그램 만들기

> 문과 중졸도 이해할 수 있게

## 📖 목차
1. 초심자를 위한 용어집
2. 과제 해석 및 분석
3. 과제를 진행하기 위한 기초
4. 각 기초를 익히기 위한 간단한 체험 예제
5. 과제를 작게 쪼개기
6. 워크플로우별 트레이드오프, 이슈, 트러블슈팅
7. 과제 완료 후 학습한 내용 정리

## 1. 초심자를 위한 용어집

| 용어 | 설명 | 비유 |
|------|------|------|
| Python | 문법이 간단한 프로그래밍 언어 | 배우기 쉬운 외국어 |
| 변수 | 데이터를 담는 상자 | 라벨 붙은 서랍 |
| 함수 | 재사용 가능한 작업 지시서 | 요리 레시피 |
| 클래스 | 데이터 + 함수를 하나로 묶음 | 명함 (이름+전화+기능) |
| JSONL | 한 줄에 하나의 JSON 객체 | 한 줄씩 적힌 가계부 |
| CRUD | Create/Read/Update/Delete | 가계부의 4가지 행동 |
| Git | 코드 변경 이력 관리 | 가계부의 수정 내역 |
| 예외 처리 | 에러 발생 시 프로그램이 죽지 않게 | 안전망 |

## 2. 과제 해석
한 줄: Python으로 용돈 기입장 프로그램을 만들고 Git으로 관리하라.
핵심: "문법을 배우는 것이 아니라 문법으로 무언가를 만드는 것"

## 3. 기초
1. Python 기본 문법 (변수, 조건, 반복, 함수)
2. 파일 I/O (open, read, write, JSON/CSV)
3. 클래스/객체 지향
4. 예외 처리 (try/except)
5. Git 기본 (add, commit, push, branch)

## 4. 체험 예제
### 파일 저장
```python
import json
data = {"date": "2026-01-01", "amount": 5000, "type": "income"}
with open("budget.jsonl", "a") as f:
    f.write(json.dumps(data) + "\n")
```

### 예외 처리
```python
try:
    amount = int(input("금액: "))
except ValueError:
    print("숫자를 입력하세요")
```

## 5. 잡/워크
- Job 1: 데이터 모델 설계 (Entry 클래스)
- Job 2: CRUD 구현 (입력/조회/수정/삭제)
- Job 3: 파일 저장 (JSONL)
- Job 4: 통계 (수입/지출 합계)
- Job 5: 예외 처리

## 6. 트레이드오프
- JSONL vs CSV: JSONL 선택 (구조화된 데이터, 확장성)
- 스택트레이스 금지: 사용자 친화적 메시지로 대체

## 7. 학습 정리
- "CRUD는 모든 앱의 기초" — 데이터 관리의 기본 패턴
- "예외 처리는 UX" — 사용자가 에러를 보면 당황
