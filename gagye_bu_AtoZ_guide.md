# 파이썬 가계부 CLI 개발 가이드: A to Z (기초부터 응용까지)

이 가이드는 파이썬 기초 문법을 갓 익힌 초보자와 컴퓨터 공학 기초 지식을 가진 분들을 위해 작성되었습니다. 이 프로젝트의 구조를 이해하고, 직접 재현하거나 새로운 기능을 추가하는 방법을 설명합니다.

---

## 1. 프로젝트 구조 이해하기: B2-1 vs B2-1-src

가장 먼저 두 폴더의 차이를 이해해야 합니다.

### B2-1-src (기초 단계: 모듈 중심)
- **특징**: 기능별로 파일을 나누었지만, 구조가 평면적입니다.
- **장점**: 코드가 한눈에 들어오고 이해하기 쉽습니다.
- **단점**: 프로그램이 커지면 코드가 엉키기 쉽고 유지보수가 어려워집니다.

### B2-1 (심화 단계: 계층형 구조 - Clean Architecture)
- **특징**: '관심사의 분리'를 극대화한 구조입니다. 
- **구조**:
    - **Models**: 데이터의 형태를 정의합니다 (가계부 내역, 카테고리 등).
    - **Interfaces**: "무엇을 할 것인가"를 정의하는 약속(규격)입니다.
    - **Repositories**: 데이터를 실제로 저장하고 불러오는 곳입니다.
    - **UseCases**: 실제 비즈니스 로직(돈을 계산하고, 필터링하는 규칙)이 들어있습니다.
    - **Presenters/Views**: 사용자에게 화면을 보여주는 방식(콘솔 출력)을 담당합니다.
- **장점**: 저장 방식을 JSON 파일에서 데이터베이스(DB)로 바꿔도 비즈니스 로직을 수정할 필요가 없습니다.

---

## 2. 기초 지식: 가계부 데이터는 어떻게 저장되나?

이 프로그램은 **JSONL (JSON Lines)** 형식을 사용합니다.
- 일반 JSON: `[{"id": 1}, {"id": 2}]` (파일 전체를 한 번에 읽어야 함)
- **JSONL**: 
  ```json
  {"id": 1, "amount": 10000}
  {"id": 2, "amount": 5000}
  ```
  한 줄이 하나의 데이터입니다. 데이터가 많아져도 한 줄씩 처리할 수 있어 효율적입니다.

---

## 3. 구현 따라하기 (A to Z)

### 1단계: 데이터 모델 만들기 (`models/`)
가장 먼저 우리가 다룰 데이터가 어떤 정보를 가질지 정합니다.
- `Transaction`: 날짜, 금액, 내용, 카테고리 ID 등.
- 파이썬의 `dataclass`를 사용하면 쉽고 깔끔하게 데이터를 정의할 수 있습니다.

### 2단계: 저장소 만들기 (`repositories/`)
데이터를 파일에 쓰고 읽는 기능을 만듭니다. 
- `jsonl_storage.py`에서 파일을 열고(`open`), 한 줄씩 읽거나(`readline`), 추가(`append`)하는 기능을 구현합니다.

### 3단계: 비즈니스 로직 구현 (`usecases/`)
가계부의 핵심 기능을 만듭니다.
- 예: "이번 달 총 지출 계산하기" -> 모든 데이터를 읽어서 날짜가 이번 달인 것만 합산합니다.

### 4단계: 사용자 인터페이스 (`views/` & `controllers/`)
사용자가 명령어를 입력하면 어떤 함수를 실행할지 연결합니다.
- `cli_controller.py`는 사용자의 입력을 받아서 적절한 `UseCase`를 호출합니다.

---

## 4. 직접 해보기 (응용 구현)

### 새로운 기능 추가하기: "검색 기능 강화"
현재는 메모 내용으로만 검색이 가능하다고 가정해봅시다. 날짜 범위(시작일~종료일)로 검색하는 기능을 추가하려면?

1.  **SearchFilters 모델 수정**: `start_date`, `end_date` 필드를 추가합니다.
2.  **TransactionUseCase 수정**: `search` 함수에서 필터를 적용할 때 날짜 비교 로직을 넣습니다.
3.  **CLI 수정**: 사용자로부터 시작일과 종료일을 입력받는 옵션을 추가합니다.

---

## 5. 실행 및 검증 방법

터미널에서 다음 명령어를 입력하여 프로그램을 실행합니다:

```bash
python3 -m budget_app
```

- **데이터 확인**: `data/` 폴더 내의 `.jsonl` 파일들을 열어보세요. 여러분이 입력한 내용이 실시간으로 저장되는 것을 볼 수 있습니다.
- **로그 확인**: 프로그램 실행 중 발생하는 오류나 성능 측정 결과는 화면에 출력되도록 설정되어 있습니다.

---

이 문서는 입문자가 현대적인 소프트웨어 설계 방식을 가계부라는 친숙한 예제로 익힐 수 있도록 돕기 위해 작성되었습니다. 구조가 복잡해 보인다면, `B2-1-src`부터 분석한 뒤 `B2-1`으로 넘어오는 것을 추천합니다.
